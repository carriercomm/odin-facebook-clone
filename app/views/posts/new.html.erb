<h1>Create Post:</h1>
<%= form_for(@post) do |f| %>
	<%= f.label :body %>
	<br>
	<%= f.text_area :body %>
	<br>
    <%= f.label :picture %>
    <br>
    <%= f.file_field :picture%>
    <br>
	<%= f.submit "Create Post", class:"btn btn-primary"%>
<% end %>